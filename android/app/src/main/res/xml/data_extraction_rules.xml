<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12 and above (android:dataExtractionRules).

     cloud-backup covers Google Backup; device-transfer covers a direct
     phone-to-phone move. Notes are the whole point of the app, so both
     include the SharedPreferences they are stored in. -->
<data-extraction-rules>
    <cloud-backup>
        <include domain="sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
