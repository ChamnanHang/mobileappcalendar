<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="ic_launcher_background">#7C5CFF</color>
    <!-- AppColors.bg. The app renders dark on every device regardless of the
         system setting, so the window and the splash use the same colour and
         there is nothing to flash. -->
    <color name="app_background">#FF06070F</color>
</resources>
